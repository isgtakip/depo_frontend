/*eslint-disable*/
import { Ability } from '@casl/ability'

export const ability = new Ability()

export default function({ store }, inject) {
//  console.log(store.state.rules.permissions)
 
  // this.$laravel.setPermissions(store.state.rules.permissions);
   
 
  }

